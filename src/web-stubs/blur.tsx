import React from 'react'
import { View, type ViewProps } from 'react-native'

export function BlurView(props: ViewProps) {
  return <View {...props} />
}

export function VibrancyView(props: ViewProps) {
  return <View {...props} />
}

export default BlurView
